# Core (classifier + processing)
numpy>=1.24
Pillow>=10.0
pdf2image>=1.16
scikit-image>=0.22

# Offline OCR (optional — serial_extract falls back if missing)
pytesseract>=0.3.10

# Web UI
flask>=3.0

# Dev / tests
pytest>=8.0
